unzip mach-bridge.zip && cd mach-bridge
./scripts/setup-ubuntu.sh   # installs Foundry, pulls dependencies, builds, runs tests
./scripts/dev-chain.sh      # starts Anvil (chain 7777) and deploys a demo setup
